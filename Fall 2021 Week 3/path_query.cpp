#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, m;
    cin >> n >> m;
    vector<int> parents(n);
    vector<int> sizes(n, 1);
    for (int i = 0; i < n; i++){
        parents[i] = i;
    }
    int total = 0;
}